pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'whoami'
                sh 'echo $PATH'
                sh './local-deploy.sh'
            }
        }
    }
}
