pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'env'
                sh 'echo $PATH'
                sh './local-deploy.sh'
            }
        }
    }
}
