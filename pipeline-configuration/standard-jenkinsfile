pipeline {
    agent any
    stages {
        stage('build') {
         environment {
               PATH = "~/app:${env.PATH}"
           }        
            steps {
                sh 'whoami'
                echo "PATH=${PATH}"
                sh 'bash -x ./local-deploy.sh'
            }
        }
    }
}
