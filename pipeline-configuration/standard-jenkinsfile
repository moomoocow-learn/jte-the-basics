pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'whoami'
                sh 'export PATH=$PATH:~/app'
                sh 'echo $PATH'
                sh './local-deploy.sh'
            }
        }
    }
}
